# Milestone 0B Prompt

The documentation foundation has been reviewed and accepted. Implement repository scaffolding only.

Create a pnpm TypeScript workspace; scaffold `apps/web`, `apps/api`, and shared packages for item-model, api-client, sync-engine, ui, design-tokens, ranking, ai, and connector-sdk. Add formatting, linting, type checking, tests, safe `.gitignore`, names-only `.env.example`, CI, and package responsibility READMEs.

Do not implement authentication, D1 schema, Notes, connectors, calendar providers, webhooks, Tauri, Android, or AI calls.

Explain dependency choices, run validation, update docs where necessary, and stop after Milestone 0B.
