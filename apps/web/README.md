# Web App

Responsibility: browser client shell for DentLink.

This app will present Notifications, Notes, Calendar, Ranking Mode, and connector settings using
shared packages. It must consume the versioned API and server-provided ranking output rather than
calculating authoritative state locally.

Milestone 0B contains only the package boundary and tooling smoke test.
