# Design Tokens

Responsibility: shared visual constants for DentLink clients.

This package will hold colors, spacing, typography, density, and motion tokens used by shared UI and
client surfaces. It must not contain app-specific layout logic or platform APIs.

Milestone 0B contains only the package boundary and tooling smoke test.
