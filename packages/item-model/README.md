# Item Model

Responsibility: shared DentLink item contracts and provider-neutral resource shapes.

This package will define the common item envelope, item types, status fields, version metadata, and
type-specific detail contracts. It must not contain connector-specific raw payload models.

Milestone 0B contains only the package boundary and tooling smoke test.
