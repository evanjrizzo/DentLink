# Item Model

Responsibility: shared DentLink item contracts and provider-neutral resource shapes.

This package defines Milestone 1 auth, session, note, folder, tag, history, sync, and conflict
contracts. It must not contain connector-specific raw payload models.
