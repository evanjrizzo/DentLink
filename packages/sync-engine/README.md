# Sync Engine

Responsibility: shared client-side sync and offline mutation queue contracts.

This package coordinates the Milestone 1 cursor sync client helper. It must not calculate
authoritative rank or bypass the backend source of truth.
